import { TablePositionDirective } from './table-position.directive';

describe('TablePositionDirective', () => {
  it('should create an instance', () => {
    const directive = new TablePositionDirective();
    expect(directive).toBeTruthy();
  });
});
