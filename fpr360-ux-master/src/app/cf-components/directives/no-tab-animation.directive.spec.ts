import { NoTabAnimationDirective } from './no-tab-animation.directive';

describe('NoTabAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new NoTabAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
